version: 2.1

jobs:
  js-test:
    docker:
      - image: gengjiawen/v8-build
    working_directory: ~/repo

    steps:
      - checkout
      - run: npx envinfo
      
workflows:
  test-ci:
    jobs:
      - js-test
