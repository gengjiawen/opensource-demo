version: 2.1

jobs:
  js-test:
    docker:
      - image: gengjiawen/v8-build
    working_directory: ~/repo

    steps:
      - checkout
      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-
      - run: npx envinfo
      
workflows:
  test-ci:
    jobs:
      - js-test
